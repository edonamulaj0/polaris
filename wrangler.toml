# Cloudflare Pages — static SPA + service binding to polaris-worker
name = "polaris"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# Proxied by functions/api/[[path]].js for /api/* requests
[[services]]
binding = "API"
service = "polaris-worker"
